"""Виды задач bschekbot: одна проба целей, VLESS-тест конфигов, скан подсети.

Лист-модуль без зависимостей: на него ссылаются сервис, статус, драйвер задач и пачки,
поэтому константы живут здесь, а не в одном из них (иначе — циклический импорт).
"""

KIND_PROBE = 'probe'
KIND_VLESS = 'vless'
KIND_SCAN = 'scan'
